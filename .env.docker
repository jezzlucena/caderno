# Docker Compose Environment Variables for Caderno

# ===========================================
# FRONTEND SERVICE ENVIRONMENT VARIABLES
# ===========================================

# Caderno Hub API URL
# Point this to your caderno-hub backend
# For local development with caderno-hub running outside Docker: http://host.docker.internal:3001/api
# For production: https://your-caderno-hub-domain.com/api
VITE_HUB_API_URL=http://host.docker.internal:3001/api

# ===========================================
# SERVER SERVICE ENVIRONMENT VARIABLES
# ===========================================

# Server Configuration
SERVER_PORT=3002
NODE_ENV=development

# Database
DATABASE_PATH=/app/data/agenda.db

# Authentication
JWT_SECRET=your-secret-key-here-change-in-production
API_KEY_SALT=your-api-key-salt-here

# Email Configuration (Optional - for scheduled exports)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# SMS Configuration (Optional - for scheduled exports)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# CORS
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FILE=/app/logs/server.log
